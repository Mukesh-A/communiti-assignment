<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <!-- <h2><%= user.users %></h2>
    <h5><%= user.enc %></h5>
    <h4><%= user.dec %></h4> -->

    <!-- <script> -->

    <% var user = user %>

    <!-- use script tag to access ejs data and local storage -->
    <script>
      let value;
        let user = <%- JSON.stringify(user) %>;
        localStorage.setItem(user.users, JSON.stringify(user));

        for (var i = 0; i < localStorage.length; i++) {
          var key = localStorage.key(i);
          value = localStorage.getItem(key);

          let display = value.replace(",", `\n`);
          // let final_val = display.replace("{", `\r\n`);

          document.writeln(display)
        }
    </script>

    <!-- </script> -->
  </body>
</html>

<!-- 
//   let keyvalue = user;
//   let user = JSON.stringify(user);
//   localStorage.setItem("info", JSON.stringify(user));
// let key = localStorage.getItem(user)
// console.log(key);

//   for (var i = 0; i < localStorage.length; i++) {
//     var key = localStorage.key(i);
//     value = localStorage.getItem(key);
//   }
//   document.writeln(value) -->
